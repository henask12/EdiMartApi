# Install devDependencies so `nest build` works, then compile.
[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node scripts/railway-start.js"
