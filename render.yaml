# Render Blueprint — EdiMart API
# Dashboard override: Root Directory must be empty (repo root), NOT "src".

services:
  - type: web
    name: edimart-api
    runtime: node
    plan: free
    rootDir: .
    buildCommand: bash build.sh
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: WEB_ORIGIN
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: SETUP_SECRET
        generateValue: true
      - key: PUBLIC_API_URL
        sync: false
      - key: UPLOAD_DIR
        value: ./uploads
